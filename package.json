{
  "name": "slides-frameworks-js",
  "version": "0.0.1",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-js": "webpack --optimize-minimize",
    "sass": "node-sass scss/main.scss dist/css/main.css",
    "watch-sass": "nodemon -e scss -x 'npm run sass'",
    "build": "npm run sass && npm run build-js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "node-sass": "3.3.0",
    "reveal.js": "^3.2.0",
    "webpack": "^1.12.10"
  }
}
